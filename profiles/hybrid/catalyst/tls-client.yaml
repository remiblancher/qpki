# =============================================================================
# Hybrid Catalyst TLS Client Certificate Profile (ECDSA P-256 + ML-DSA-65)
# =============================================================================
#
# Usage: Hybrid TLS client certificate for mTLS with PQC transition
#
# Example:
#   pki issue --profile hybrid/catalyst/tls-client --cn "alice@acme.fr"
#
# =============================================================================

name: hybrid/catalyst/tls-client
description: "TLS client hybrid ECDSA P-256 + ML-DSA-65 (PQC transition)"

mode: catalyst
algorithms:
  - ecdsa-p256
  - ml-dsa-65

validity: 365d

subject:
  fixed:
    c: "FR"
    o: "ACME Industrie"
    ou: "Utilisateurs"
  required:
    - cn
  optional:
    - email
    - serialNumber

extensions:
  basicConstraints:
    critical: true
    ca: false

  keyUsage:
    critical: true
    values:
      - digitalSignature

  extKeyUsage:
    critical: false
    values:
      - clientAuth

  subjectAltName:
    critical: false
    email:
      - "${EMAIL}"

  certificatePolicies:
    critical: false
    policies:
      - oid: "1.3.6.1.4.1.99999.1.3.4"
        cps: "https://pki.acme-industrie.fr/cps"

  crlDistributionPoints:
    urls:
      - "http://crl.acme-industrie.fr/issuing-ca-hybrid.crl"

  authorityInfoAccess:
    caIssuers:
      - "http://pki.acme-industrie.fr/certs/issuing-ca-hybrid.crt"
    ocsp:
      - "http://ocsp.acme-industrie.fr"
