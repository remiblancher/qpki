# Hybrid Catalyst Root CA profile
# Root CA with combined ECDSA P-384 + ML-DSA-87 signatures

name: hybrid/catalyst/root-ca
description: "Root CA hybrid ECDSA P-384 + ML-DSA-87 (PQC transition)"

signature:
  required: true
  mode: hybrid-combined
  algorithms:
    primary: ecdsa-p384
    alternative: ml-dsa-87

encryption:
  required: false
  mode: none
  algorithms: {}

validity: 7300d  # 20 years

extensions:
  keyUsage:
    critical: true
    values:
      - keyCertSign
      - cRLSign

  basicConstraints:
    critical: true
    ca: true
    pathLen: 1
